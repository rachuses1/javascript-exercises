const getTheTitles = function(books) {

    // let revised = [];

    // for(let i = 0; i < books.length; i++) {
    //     revised.push(books[i].title)
    // }

    return books.map((book) => book.title)

// return revised
};

// Do not edit below this line
module.exports = getTheTitles;
